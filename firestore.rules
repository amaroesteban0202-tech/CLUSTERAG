rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // La aplicacion usa la API del servidor. Firestore no es una via de acceso.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
